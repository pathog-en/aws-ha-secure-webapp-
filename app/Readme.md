## Local Container Run (No AWS Required)

This project includes a containerized sample web service to demonstrate how the application tier can be packaged for deployment (e.g., ECS/Fargate later).

### Run locally
```bash
docker compose up --build
